export interface User {
    id?: number; // Asegúrate de que coincida con el tipo que estás usando para ID
    username?: string;
    firstname?: string;
    lastname?: string;
    email?: string;
    password?: string;
    // Otras propiedades si es necesario
  }